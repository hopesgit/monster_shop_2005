<h1>This is a space for the All-Powerful to rest!</h1>
<br>

<section id="packaged-orders">
  <h2>Packaged Orders:</h2>
  <% if @orders.packaged_orders.empty? %>
    <p>Nothing to see here.</p>
  <% else %>
    <% @orders.packaged_orders.each do |order| %>
      <section id="order-<%= order.id %>">
        <p>User name: <%= link_to "#{order.user.name}", "/admin/users/#{order.user.id}" %></p>
        <p>Order ID: <%= order.id %></p>
        <p>Date created: <%= order.created_at.localtime.strftime("%m/%d/%y") %></p>
        <%= button_to "Ship", admin_order_path(order.id), method: :patch %>
        <p>---------</p>
      </section>
    <% end %>
  <% end %>
</section>

<section id="pending-orders">
  <h2>Pending Orders:</h2>
  <% if @orders.pending_orders.empty? %>
    <p>Nothing to see here.</p>
  <% else %>
    <% @orders.pending_orders.each do |order| %>
      <section id="order-<%= order.id %>">
        <p>User name: <%= link_to "#{order.user.name}", "/admin/users/#{order.user.id}" %></p>
        <p>Order ID: <%= order.id %></p>
        <p>Date created: <%= order.created_at.localtime.strftime("%m/%d/%y") %></p>
        <%= button_to "Ship", admin_order_path(order.id), method: :patch %>
        <p>---------</p>
      </section>
    <% end %>
  <% end %>
</section>

<section id="shipped-orders">
  <h2>Shipped Orders:</h2>
  <% if @orders.shipped_orders.empty? %>
    <p>Nothing to see here.</p>
  <% else %>
    <% @orders.shipped_orders.each do |order| %>
      <section id="order-<%= order.id %>">
        <p>User name: <%= link_to "#{order.user.name}", "/admin/users/#{order.user.id}" %></p>
        <p>Order ID: <%= order.id %></p>
        <p>Date created: <%= order.created_at.localtime.strftime("%m/%d/%y") %></p>
        <%= button_to "Ship", admin_order_path(order.id), method: :patch %>
        <p>---------</p>
      </section>
    <% end %>
  <% end %>
</section>

<section id="cancelled-orders">
  <h2>Cancelled Orders:</h2>
  <% if @orders.cancelled_orders.empty? %>
    <p>Nothing to see here.</p>
  <% else %>
    <% @orders.cancelled_orders.each do |order| %>
      <section id="order-<%= order.id %>">
        <p>User name: <%= link_to "#{order.user.name}", "/admin/users/#{order.user.id}" %></p>
        <p>Order ID: <%= order.id %></p>
        <p>Date created: <%= order.created_at.localtime.strftime("%m/%d/%y") %></p>
        <%= button_to "Ship", admin_order_path(order.id), method: :patch %>
        <p>---------</p>
      </section>
    <% end %>
  <% end %>
</section>
